{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
  "name": "GEMINI_API_KEY",
  "description": "Google Gemini API key for AI-powered stream analysis and marketing copy generation. Optional — the app has built-in fallbacks without it. Obtain from Google AI Studio (aistudio.google.com).",
  "requiredAtBoot": false
}
  ]
}
